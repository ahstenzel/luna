{
  "version": "2.0.0",
  "tasks": [
      {
          "label": "Scan for compilers",
          "command": "${command:cmake.scanForCompilers}",
          "runOptions": {
              "runOn": "folderOpen",
          }
      }
  ]
}